import { InputPointerDirective } from './input-pointer.directive';

describe('InputPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPointerDirective();
    expect(directive).toBeTruthy();
  });
});
